# Upload packages built by scripts/package_skills.py and the release workflow.
dist/

# Output of scripts/eval.py: per-run transcripts, judge output, and eval.json.
# These are evaluation records of one model on one day, not source.
tests/results/

.DS_Store

# Local Python environment and bytecode.
.venv/
__pycache__/
*.py[cod]
